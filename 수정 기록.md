# 앱 수정 및 빌드 기록

이 파일은 앱의 수정 사항, 검토 결과, 사전 테스트 및 정식 빌드 이력을 기록합니다.

## 2026-09-10

### 기준 버전

- 현재 운영 빌드 버전: **2.6.19**
- 현재 버전은 여러 사용자가 사용 중인 안정 버전이므로, 후속 수정 시 기존 버전을 보존합니다.

### 합의된 업데이트 및 테스트 방식

- 새 기능 또는 수정 사항은 운영 버전과 분리하여 검토합니다.
- 수정 후 별도의 사전 테스트 빌드를 생성합니다.
- 사전 테스트 빌드는 `2.6.20-beta` 또는 `2.6.20-test`와 같이 구분합니다.
- 기존 설치 및 운영 데이터에 영향을 주지 않도록 별도 폴더와 테스트 데이터 사용을 우선합니다.
- 사용자의 사전 테스트가 완료되고 오류 및 기존 기능 회귀 문제가 없을 때 정식 빌드로 반영합니다.
- 최종 정식 버전은 변경 내역, 테스트 결과, 빌드 일시와 함께 이 파일에 기록합니다.

### 현재 상태

- 상태: **v2.7.0 릴리즈 준비 완료**
- 이전 운영 빌드: `2.6.19`
- 정식 릴리즈: `v2.7.0`

## 2026-09-11 — 버전 v2.7.0

- 유형: 연도 선택 기능 추가 정식 릴리즈
- 수정 내용:
  - 달력의 `2026년 9월` 월 제목을 클릭하면 현재 연도 주변 11개 연도를 선택할 수 있도록 추가
  - 선택한 연도의 같은 달로 이동하고, 해당 연도의 1일을 선택 날짜로 표시
  - 연도 선택기는 달력 패널 안의 컬러풀한 팝오버로 구성하고 현재 연도를 강조
  - 기존 할일 저장 데이터 구조와 목록 렌더링은 변경하지 않음
- 검토 내용:
  - 연도 선택, 월 이동, 오늘 이동, 날짜별 할일 표시의 기존 흐름과 충돌하지 않도록 이벤트 분리
  - 버전 업데이트 후에도 사용자 데이터 저장 위치(`app.getPath('userData')`)와 파일명(`todos.json`)을 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (9개)
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.7.0.exe`
- 비고:
  - GitHub `v2.7.0` 릴리즈에 설치 파일과 업데이트 메타데이터를 업로드

### 2026-09-10 — 버전 2.6.20-beta.1

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 할일 위젯 하단에 달력 리본 버튼 추가
  - 달력 리본 선택 시 할일 목록과 달력을 함께 표시하는 확장 화면 추가
  - 달력 화면 닫기 시 기존 할일 위젯 화면으로 복귀
  - 기본 캐릭터는 위젯 하단에 계속 표시하고, 달력 화면에는 선택 캐릭터가 오른쪽 아래에서 보이도록 구성
  - 설정에서 선택한 캐릭터가 기본 영역과 달력 장식 영역에 함께 반영되도록 연결
  - 달력 월 이동, 오늘 날짜 이동, 날짜별 할일 표시 기능 추가
- 검토 내용:
  - 운영 버전 2.6.19와 분리된 `dist/win-unpacked` 베타 실행 파일 및 별도 테스트 데이터로 검증
  - 실제 Windows 화면에서 캐릭터 선택, 달력 열기, 월 이동, 달력 닫기 흐름 확인
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 410×575 할일 위젯 화면 확인
  - 800×720 달력 확장 화면 확인
  - 달력 닫기 후 할일 위젯만 표시되고 원래 캐릭터가 유지되는 것 확인
- 알려진 문제:
  - 베타 실행 중 기존 자동 업데이트 확인 팝업이 표시될 수 있음. 테스트에서는 업데이트 시작을 누르지 않고 닫았으며, 달력 기능 검증에는 영향이 없었음.
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.1.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음. 사용자 사전 테스트 후 정식 반영 여부를 결정함.

### 2026-09-10 — 버전 2.6.20-beta.2

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 달력 확장 화면을 첨부 디자인 방향에 맞춰 좌측 할일·우측 달력의 컬러풀한 카드형 레이아웃으로 개선
  - 둥근 카드, 하늘색·핑크·민트·보라색 포인트, 달력 날짜 원형 강조 스타일 적용
  - 할일 추가·완료·삭제·설정·동기화 버튼을 확장 화면에 맞는 알약형 UI로 정리
  - 확장 화면의 기존 말풍선이 콘텐츠를 가리지 않도록 숨김 처리
  - 할일 목록과 하단 달력 리본 사이에 여백을 확보해 카드가 가려지지 않도록 조정
  - 달력 오른쪽 아래 선택 캐릭터 엿보기와 기본 캐릭터 하단 표시 유지
- 검토 내용:
  - 운영 버전 2.6.19와 분리된 실행 파일 및 별도 테스트 데이터로 실제 Windows 화면 검증
  - 기본 위젯 → 달력 확장 → 달력 닫기 흐름 확인
  - 테스트 데이터가 있는 화면에서 할일 카드와 달력 패널의 가독성 확인
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 410×575 기본 할일 위젯 화면 확인
  - 800×720 좌우 확장 화면 확인
  - 컬러풀한 패널, 달력 날짜 강조, 두 위치의 캐릭터 표시 확인
  - 달력 닫기 후 할일 위젯만 표시되고 기본 캐릭터가 유지되는 것 확인
- 알려진 문제:
  - 베타 실행 중 기존 자동 업데이트 확인 팝업이 표시될 수 있음. 테스트에서는 업데이트 시작을 누르지 않았음.
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.2.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음. 사용자 사전 테스트 후 정식 반영 여부를 결정함.

### 2026-09-10 — 버전 2.6.20-beta.3

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 이미지 1의 달력 UI 방향에 맞춰 달력 패널을 더 조밀하게 재배치
  - 선택한 날짜와 날짜별 할일 카드가 달력 패널 내부 간격 안에 들어오도록 달력 행 높이와 flex 레이아웃 조정
  - 달력 패널 가로폭을 370px에서 410px로 확대하고 확장 창 너비를 840px로 조정
  - 확장 화면에서 기본 캐릭터를 전체 창의 가로 중앙으로 이동
  - 달력 오른쪽 아래 장식 캐릭터는 단일 이미지 한 장만 사용하고 기존 깜빡임·부유 애니메이션 제거
  - 장식 캐릭터가 이미지 1처럼 오른쪽 아래에서 자연스럽게 보이도록 고정 위치와 노출 영역 조정
- 검토 내용:
  - 운영 버전 2.6.19와 분리된 `C:\\tmp` 임시 패키지 및 별도 사용자 데이터로 실제 Windows 화면 검증
  - 기본 위젯 → 좌우 달력 확장 → 달력 닫기 흐름 확인
  - 선택 날짜 카드가 달력 패널 밖으로 밀려나지 않는지 확인
  - 중앙 기본 캐릭터와 오른쪽 아래 정적 장식 캐릭터의 위치 확인
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 410×575 기본 위젯 화면 확인
  - 840×720 좌우 확장 화면 확인
  - 선택 날짜 카드가 달력 패널 내부에 표시되는 것 확인
  - 기본 캐릭터 중앙 배치 및 달력 장식 캐릭터 정적 표시 확인
  - 달력 닫기 후 할일 위젯으로 복귀하는 것 확인
- 알려진 문제:
  - Z드라이브의 기존 `dist\\win-unpacked` 폴더가 동기화 잠금 상태여서 빌드 결과 폴더 복사는 실패할 수 있음. 설치 파일과 임시 패키지는 생성됨.
  - 현재 설치 파일은 운영 버전과 동일한 앱 ID를 사용하므로 2.6.19가 설치된 PC에 직접 설치하지 않음. 임시 실행 방식으로만 사전 검증함.
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.3.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음. 운영 PC 공유용으로는 별도 앱 ID를 적용한 설치 파일을 추가로 만들어야 함.

### 2026-09-10 — 버전 2.6.20-beta.4

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 달력 캐릭터 영역을 달력 패널 하단의 고정 클리핑 영역으로 변경
  - 선택 캐릭터의 단일 정적 이미지가 달력 패널의 둥근 카드 경계 안에서만 보이도록 조정
  - 캐릭터가 패널 아래 바탕 화면으로 튀어나오던 문제 수정
  - 기존 달력 확장 화면의 중앙 기본 캐릭터와 선택 날짜 카드 배치 유지
- 검토 내용:
  - 운영 버전 2.6.19와 분리된 `C:\\tmp` 임시 패키지 및 별도 사용자 데이터로 실제 Windows 화면 검증
  - 달력 열기 시 달력 캐릭터가 패널 내부 하단에만 표시되는지 확인
  - 달력 닫기 후 기본 할일 위젯과 중앙 캐릭터로 복귀하는지 확인
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 840×720 좌우 확장 화면에서 선택 날짜 카드가 패널 내부에 표시되는 것 확인
  - 달력 캐릭터가 패널 밖으로 돌출되지 않고 하단 영역 안에서 잘리는 것 확인
  - 달력 캐릭터 애니메이션 없이 정적 이미지로 표시되는 것 확인
  - 달력 닫기 후 기본 위젯으로 복귀하는 것 확인
- 알려진 문제:
  - Z드라이브의 기존 `dist\\win-unpacked` 폴더 동기화 잠금으로 빌드 결과 폴더 복사는 실패할 수 있음. 임시 패키지와 설치 파일은 생성됨.
  - 현재 설치 파일은 운영 버전과 동일한 앱 ID를 사용하므로 2.6.19가 설치된 PC에 직접 설치하지 않음.
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.4.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음. 사용자 사전 테스트 후 별도 앱 ID 베타 배포본 제작 여부를 결정함.

### 2026-09-10 — 버전 2.6.20-beta.5

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 달력 캐릭터 영역에 별도로 칠해진 배경 그라데이션을 제거
  - 캐릭터 왼쪽에 보이던 희미한 세로 경계선이 생기지 않도록 조정
  - 달력 캐릭터는 기존처럼 한 장의 정적 이미지로 표시하고 패널 안쪽 클리핑 유지
- 검증:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과
  - 별도 임시 사용자 데이터로 실제 Windows 화면에서 달력 열기 및 닫기 확인
  - 달력 캐릭터가 패널 밖으로 튀어나오지 않고 왼쪽 경계선 없이 표시되는지 확인
- 알려진 문제:
  - Z드라이브의 기존 `dist\\win-unpacked` 폴더 동기화 잠금으로 빌드 결과 폴더 복사는 실패할 수 있음. 임시 패키지와 설치 파일은 생성됨.
  - 현재 설치 파일은 운영 버전과 동일한 앱 ID를 사용하므로 2.6.19가 설치된 PC에 직접 설치하지 않음.
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.5.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.

### 2026-09-10 — 버전 2.6.20-beta.6

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 하단 리본에 검색 아이콘과 검색·필터 패널 추가
  - 할일 제목과 서브할일을 대상으로 검색하도록 구현
  - 전체, 오늘, 진행 중, 완료, 마감 임박(72시간 이내), 기한 지남 필터 추가
  - 검색 조건에 맞는 결과 수와 결과 없음 안내 표시
  - 기존 완료 목록 보기, 달력, 캐릭터 동작과 함께 사용할 수 있도록 연결
- 검토 내용:
  - 검색·필터 패널을 기존 할일 창 안에서 접고 펼치는 방식으로 구성
  - 달력 확장 시에도 왼쪽 할일 패널에서 검색 기능을 사용할 수 있도록 반영
- 실제 Windows 검증:
  - 검색 패널 열기 및 3열 2행 필터 배치 확인
  - `마감 임박` 필터 선택 시 선택 상태와 결과 수 갱신 확인
  - 검색 패널을 연 상태에서 달력 확장 화면이 함께 표시되는 것 확인
- 테스트:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.6.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음. 베타 테스트 후 정식 반영 여부를 결정함.

### 2026-09-10 — 버전 2.6.20-beta.7

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 검색·필터 패널을 할일 목록 내부가 아닌 우측 독립 패널로 이동
  - 검색 화면에서 창 폭을 700px로 확장하고 왼쪽 할일 목록 표시 영역 유지
  - 검색 패널에 제목, 닫기 버튼, 검색 결과 안내 추가
  - 검색과 달력은 한 번에 하나만 열리도록 화면 전환 구조 정리
  - 확장 화면에서 기본 캐릭터를 전체 창 중앙에 배치
- 검토 내용:
  - 검색 패널을 달력과 동일한 좌우 확장 방식으로 설계
  - 기존 할일 목록의 입력·완료·수정·삭제 영역은 유지
- 테스트:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 실제 Windows 화면에서 좌측 할일 패널과 우측 검색 패널 동시 표시 확인
  - 필터 선택 상태 및 결과 수 갱신 확인
  - 검색 화면에서 달력 화면으로 전환 확인
- 비고:
  - 달력 전환 시 검색 카운트가 남는 표시 문제를 beta.8에서 보완함.

### 2026-09-10 — 버전 2.6.20-beta.8

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 검색 화면에서 달력으로 전환할 때 검색 조건·필터·검색 카운트를 초기화
  - 달력 전환 후 왼쪽 목록 카운트가 `할일 n개` 형식으로 정상 복귀하도록 수정
- 실제 Windows 검증:
  - 별도 임시 사용자 데이터로 beta.8 실행
  - 검색 리본 선택 시 700×575 좌우 확장 화면 및 좌측 목록 공간 유지 확인
  - 검색 패널의 3열 2행 필터 배치와 `마감 임박` 선택 상태 확인
  - 검색에서 달력 전환 후 우측 달력 표시 및 왼쪽 카운트 정상 복귀 확인
  - 달력의 선택 날짜 카드와 하단 정적 캐릭터 표시 확인
- 테스트:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
- 빌드 일시: `2026-09-10 16:00:46 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.8.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 운영 버전과 동일한 앱 ID이므로 기존 2.6.19 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-10 — 버전 2.6.20-beta.9

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 검색 모드의 왼쪽 할일 패널을 고정 높이 360px로 변경
  - 검색 결과 수에 따라 패널 외곽 크기가 변하지 않도록 수정
  - 검색 결과가 많거나 없을 때에도 왼쪽 창 크기를 동일하게 유지
  - 검색 조건에 맞는 할일만 왼쪽 목록 내부에 표시
- 실제 Windows 검증:
  - 검색 결과 5개 표시 상태에서 왼쪽 패널 고정 크기 확인
  - 필터 결과 0개 상태에서도 동일한 왼쪽 패널 크기 유지 확인
  - 우측 검색 패널과 좌측 목록의 좌우 배치 유지 확인
- 테스트:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
- 빌드 일시: `2026-09-10 16:11:05 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.9.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 운영 버전과 동일한 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.12

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 눈뜬 기본 이미지와 눈감은 기본 이미지가 겹치며 남기던 초기 이미지 잔상 제거
  - CSS opacity 타임라인 대신 JS `is-blinking` 상태로 완전한 이미지 레이어를 원자적으로 교체
  - 눈깜빡임 중 한 캐릭터 레이어만 표시해 잔상과 빈 프레임이 생기지 않도록 조정
  - 기존 캐릭터 부유 애니메이션은 유지하고, 사용자 선택 캐릭터·기한 초과 캐릭터에는 눈깜빡임을 적용하지 않도록 분리
- 검토 내용:
  - beta.9에서 보고된 잔상 현상을 재검토하고, 눈뜬/눈감은 이미지가 모두 전체 캐릭터 렌더라는 점을 확인
  - 기존 CSS 중첩 전환에서 전환 프레임의 잔상 및 캐릭터가 잠시 사라질 가능성을 확인한 뒤 레이어 교체 구조로 변경
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 실제 Windows 임시 실행본에서 초기 눈뜬 화면 확인
  - 연속 캡처 중 눈감은 프레임 확인
  - 눈깜빡임 중 초기 이미지 잔상, 캐릭터 중첩, 빈 프레임이 발생하지 않는 것 확인
- 빌드 일시: `2026-09-11 10:04:29 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.12.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 운영 버전과 동일한 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.13

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 눈깜빡임 이미지 레이어에 적용되던 `opacity` 페이드 전환 제거
  - 앞 이미지가 즉시 사라지고 뒷 이미지가 즉시 표시되도록 변경
  - 캐릭터의 기존 부유 애니메이션은 유지
- 검토 내용:
  - beta.12에서 잔상은 없어졌지만 이미지 교체 중 흰색으로 보이는 페이드아웃 현상 확인
  - `.character-img`의 opacity transition을 제거하고 이미지 자세 전환과 캐릭터 움직임을 분리
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (4개)
  - 실제 Windows 임시 실행본에서 눈뜬 프레임과 눈감은 프레임 확인
  - 연속 화면에서 중간 흰색 페이드 프레임 없이 즉시 교체되는 것 확인
- 빌드 일시: `2026-09-11 12:43:51 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.13.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 운영 버전과 동일한 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.15

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 할일별 마감 전 Windows 알림 설정 추가
  - 5분·10분·30분·1시간·하루 전 알림 선택 지원
  - 매일·평일·매주·매월 반복 일정 지원
  - 반복 할일 완료 시 다음 일정 자동 생성
  - 하단 리본의 검색·달력·알림 버튼을 컬러 스타일로 구성
  - 사용자 화면 명칭을 `알림`으로 통일하고, 반복은 설정 항목 `반복`으로 유지
- 검토 내용:
  - 기존 할일·달력·검색·필터·캐릭터 동작은 변경하지 않음
  - 알림 설정은 기존 달력·검색과 같은 오른쪽 확장 패널로 분리
  - 기존 할일 데이터에는 기본값을 적용해 호환 유지
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - 실제 Windows 임시 실행본에서 새 beta15 실행 파일과 컬러 리본을 확인
  - 운영 설치본과 분리된 임시 프로필로 실행
- 빌드 일시: `2026-09-11 13:15:22 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.15.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.16

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 알림 패널 하단 안내 문장이 영역 밖으로 밀려나는 문제 수정
  - 패널 내부 간격과 선택 상자 높이를 조정해 고정 높이 안에 배치
  - 안내 문장 자동 줄바꿈 및 패널 내부 넘침 방지 적용
- 검토 내용:
  - 알림·반복 기능 범위 안의 CSS만 수정
  - 기존 할일 목록·달력·검색·필터·캐릭터 동작은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - beta16 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 13:45:20 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.16.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.17

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 말풍선이 하단 리본 영역을 가리던 위치 문제 수정
  - 말풍선을 캐릭터 앞쪽으로 이동해 캐릭터와 겹쳐 표시
  - 말풍선 레이어를 캐릭터보다 앞에 배치
- 검토 내용:
  - 말풍선 위치와 레이어 관련 CSS만 수정
  - 알림·반복, 할일 목록, 달력, 검색·필터 기능은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - beta17 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 13:56:47 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.17.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.18

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 말풍선을 캐릭터와 겹치지 않는 왼쪽 위 영역으로 이동
  - 만화책 형태의 둥근 말풍선과 캐릭터 방향 꼬리 적용
  - 긴 문장이 여러 줄로 표시되도록 줄바꿈 적용
  - 기존 응원 문장의 이모티콘 표시 유지
- 검토 내용:
  - 말풍선 위치·모양·레이어 관련 CSS만 수정
  - 알림·반복, 할일 목록, 달력, 검색·필터 기능은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - beta18 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 15:24:13 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.18.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.19

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 말풍선을 화면 위쪽이 아닌 캐릭터 바로 왼쪽 앞 공간으로 이동
  - 캐릭터와 말풍선 사이의 불필요한 간격을 줄임
  - 말풍선 꼬리가 캐릭터 방향을 향하도록 유지
- 검토 내용:
  - 말풍선 위치 관련 CSS만 수정
  - 알림·반복, 할일 목록, 달력, 검색·필터 기능은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - beta19 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 15:30:12 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.19.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.20

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 녹색 완료 버튼의 바깥 배경은 기존처럼 유지
  - 미완료 상태의 녹색 네모 안에 연한 회색 빈 체크박스 추가
  - 완료 처리 중에는 기존 녹색 버튼과 흰색 체크 표시 유지
- 검토 내용:
  - 완료 버튼의 시각적 표시 CSS만 수정
  - 클릭 동작, 할일 데이터, 알림·반복, 달력, 검색·필터 기능은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (7개)
  - beta20 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 15:43:25 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.20.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

### 2026-09-11 — 버전 2.6.20-beta.21

- 유형: 사전 테스트 빌드
- 수정 내용:
  - 캐릭터와 기본 할일 목록을 기본 영역의 고정 좌표에 유지
  - 검색·달력·알림 패널을 오른쪽에 추가하는 방식으로 레이아웃 변경
  - 패널 전환 시 캐릭터를 창 중앙으로 이동시키던 규칙 제거
  - 창 크기 변경 시 왼쪽 기준 위치를 유지하고, 화면 밖으로 나갈 때만 필요한 만큼 보정
- 검토 내용:
  - 기본 영역의 할일·캐릭터 위치와 오른쪽 기능 패널 배치 관련 코드만 수정
  - 할일 데이터, 알림·반복 동작, 캐릭터 애니메이션은 변경하지 않음
- 테스트 결과:
  - `npm run lint:js` 통과
  - `node --test tests/*.test.js` 통과 (8개)
  - beta21 실행 파일 생성 및 별도 임시 프로필 실행 확인
- 빌드 일시: `2026-09-11 15:54:01 +09:00`
- 빌드 파일:
  - `dist\\Character-Todo-Google-Setup-2.6.20-beta.21.exe`
- 비고:
  - 정식 운영 빌드 2.6.19는 변경하지 않음.
  - 동일 앱 ID이므로 기존 설치 PC에 바로 설치하지 않고 임시 실행 방식으로 사전 테스트함.

## 빌드 기록 양식

### YYYY-MM-DD — 버전 X.X.X

- 유형: 사전 테스트 / 정식 빌드
- 수정 내용:
- 검토 내용:
- 테스트 결과:
- 알려진 문제:
- 빌드 파일:
- 비고:
